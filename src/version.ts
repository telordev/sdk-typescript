export const VERSION = "1.0.0";

/** The Anthropic-style API version header sent on every request. */
export const DEFAULT_API_VERSION = "2026-06-01";

/** Default base URL for the Simse public API. */
export const DEFAULT_BASE_URL = "https://api.simse.dev";
